import React from "react";
import { Tilt } from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { StarWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img
          src={icon}
          alt="web-development"
          className="w-16 h-16 object-contain"
        />
        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        👋 Hi there! I'm Prince Kumar, a passionate and results-driven web developer with over 2 years of experience specializing in Full Stack Development using the MERN stack. My journey in the web development world started with a love for creating visually appealing and intuitive user interfaces. Over time, I've evolved into a full-stack developer, mastering the art of turning ideas into robust, scalable, and efficient web applications.

        As the backend developer of PaediPrime, a pioneering startup focused on revolutionizing pediatric healthcare, I have contributed to streamlining the booking and checkup scheduling process, saving both time and effort for parents and clinics. Our mission at PaediPrime is to transform pediatric care by making it more accessible and efficient.
      </motion.p>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        Beyond web development, I am deeply passionate about solving complex problems, which I demonstrate through competitive programming, having solved over 100 problems on LeetCode. My problem-solving skills are continuously honed through participating in coding challenges and competitive programming events, allowing me to tackle challenging scenarios with creative and efficient solutions.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default StarWrapper(About, "about");
